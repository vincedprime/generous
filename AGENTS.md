# Project instructions

This directory is the canonical source for Generous. Make all future website changes here, including data, styles, scripts, and assets. Preserve the existing GitHub origin. The website is buildless static HTML/CSS/JavaScript under `dist/`. Keep the existing Sites project ID when publishing. Never commit hosting credentials. Keep logos local and record asset provenance. Preserve the draft-ranking disclosure until the underlying catalog has been verified.
